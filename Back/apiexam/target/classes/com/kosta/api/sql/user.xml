<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 
<mapper namespace="mapper.user">
	<!-- 일반 회원가입 -->
	<insert id="insertUser" parameterType="UserInfo">
		<![CDATA[
			insert into users (id, password, name, nickname, email, address, mobile, profileImage) 
			values (#{id},#{password},#{name},#{nickname},#{email},#{address},#{mobile},#{profileImage})
		]]>
	</insert>
	<!-- kakao -->
	<insert id="insertUserByKakao" parameterType="UserInfo">
		<![CDATA[
			insert into users (id, nickname, email, gender, profileImage, thumbnailImage) 
			values (#{id},#{nickname},#{email},#{gender},#{profileImage},#{thumbnailImage})
		]]>
	</insert>
	<!-- naver -->
	<insert id="insertUserByNaver" parameterType="UserInfo">
		<![CDATA[
			insert into users (id, name, nickname, email, gender, mobile, profileImage, age, birthday) 
			values (#{id},#{name},#{nickname},#{email},#{gender},#{mobile},#{profileImage}, #{age}, #{birthday})
		]]>
	</insert>

	<!-- 회원정보 -->
	<select id="selectUser" parameterType="String" resultType="UserInfo">
		<![CDATA[
			select * from users where id=#{id}
		]]>
	</select>
	
	<!-- 회원정보 변경 -->
	<update id="updateUser" parameterType="UserInfo">
		<![CDATA[
			
		]]>
	</update>
	
</mapper>